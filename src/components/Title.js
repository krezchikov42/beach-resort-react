import React from 'react'

const Title = ({title}) => {
    return (
        <div className="section-title">
            <h4>{title}</h4>
            <div className="horizontal-line"/>
        </div>
    )
}

export default Title
